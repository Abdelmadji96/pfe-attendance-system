requests
python-dotenv
spidev
smbus2==0.5.0
# mfrc522 installed with --no-deps (see setup below) — its pip dependency pulls broken RPi.GPIO on Pi 5
# Pi 5: sudo apt install python3-rpi-lgpio
#       pip install mfrc522 --no-deps
#       pip uninstall RPi.GPIO -y
# Pi 4: pip install RPi.GPIO mfrc522
