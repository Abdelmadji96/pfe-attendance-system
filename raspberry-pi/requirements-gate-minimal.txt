# Minimal gate deps — hardware tests, RFID, LCD, API (no TensorFlow / OpenCV)
# Works on Pi OS Trixie / Python 3.13

python-dotenv>=1.0.1
requests>=2.32.0
urllib3>=2.0.0
numpy>=2.0.0
spidev>=3.6
mfrc522==0.0.7
smbus2==0.5.0
coloredlogs>=15.0.0
humanfriendly>=10.0

# GPIO: sudo apt install python3-rpi-lgpio && venv --system-site-packages
