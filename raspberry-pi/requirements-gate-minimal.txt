# Minimal gate deps — hardware tests, RFID, LCD, API (no TensorFlow / OpenCV)
# Works on Pi OS Trixie / Python 3.13

python-dotenv>=1.0.1
requests>=2.32.0
urllib3>=2.0.0
numpy>=2.0.0
spidev>=3.6
# mfrc522 installed with --no-deps in setup-gate-env.sh (avoids broken pip RPi.GPIO on Pi 5)
mfrc522==0.0.7
smbus2==0.5.0
coloredlogs>=15.0.0
humanfriendly>=10.0

# Pi 5 GPIO (required in venv when using custom Python 3.11; apt python3-rpi-lgpio is for system 3.13 only)
rpi-lgpio>=0.6
